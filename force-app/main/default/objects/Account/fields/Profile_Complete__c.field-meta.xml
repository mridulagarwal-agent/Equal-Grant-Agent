<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Profile_Complete__c</fullName>
    <description>Gate for Grant_Discovery topic. True when core intake fields are all populated.</description>
    <externalId>false</externalId>
    <formula>AND(
    NOT(ISBLANK(NAICS_Code__c)),
    NOT(ISBLANK(Industry_Keywords__c)),
    NOT(ISBLANK(TEXT(Operating_State__c))),
    NOT(ISBLANK(TEXT(Revenue_Band__c))),
    NOT(ISBLANK(TEXT(Business_Stage__c)))
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Profile Complete</label>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
